# (User) Service Unit for KeePassXC
[Unit]
Description=KeePassXC with automatic restart
After=graphical.target

[Service]
Type=exec
WorkingDirectory=%h
ExecStart=/usr/bin/keepassxc -platform xcb
Restart=on-failure
RestartSec=5s

[Install]
WantedBy=default.target

